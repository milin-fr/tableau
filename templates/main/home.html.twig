{% extends 'base.html.twig' %}

{% block title %}Equipes{% endblock %}

{% block body %}

<div class="container">
  <div class="row">
    {% for team in teams %}
        <div class="col-sm">
            <a href="{{ path('team_home', {'id': team.id}) }}">{{ team.title }}</a>
        </div>
    {% else %}
        <div class="col-sm">
            Y'as pas d'equipes. <a href="work_team_new">Il faut en creer une</a>.
        </div>
    {% endfor %}
  </div>
  <a href="{{ path('project_add_task', {'id': 6}) }}"class="test-test">test</a>
</div>
{% endblock %}
